var username = [];
if (username.length === 0) {
    console.log("We need to find some users!");
}
else {
    // Greet user
}
// Removing are users namenames ensures the message "We need to find some users! is Printed"
